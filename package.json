{
  "name": "nexus-tib",
  "version": "1.0.0",
  "description": "Nexus Ticket Information Builder — GPP / VNG Games",
  "private": true,
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
